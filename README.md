# My *first or something like that* app
Install requirements
1. pip install -r requirements.txt

or

pip install -r requirements.txt --user

2. Run app
streamlit run main.py

built with 💗 and streamlit.